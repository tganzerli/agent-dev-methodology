# Wiki Log

> Chronological, append-only. Standard prefix `## [YYYY-MM-DD] {operation} | {target}` so entries stay `grep`-able.
>
> Possible operations: `bootstrap`, `ingest`, `query`, `wiki-sync`, `lint`, `methodology-update`, `git-correction`, `rename`.
>
> Newest entries go on **top**, right below this header. Never edit or delete a past entry — only append.

<!--
FORMAT SAMPLE (not a real entry — delete this block once the first real
entry is appended):

## [YYYY-MM-DD] wiki-sync | {work_id} | N pages touched

One or two sentences summarizing what the execution delivered and why it
mattered. Trio at [[work/executions/{work_id}]].

- [[path/to/page]] (CREATE) — one-line description of what the new page covers.
- [[path/to/other-page]] (UPDATE) — one-line description of what changed and where.
- [[_meta/index]] (UPDATE) — registers the new/changed pages.
-->
